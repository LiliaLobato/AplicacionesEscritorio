{
  "name": "tarea4",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "estilos": "sass src/sass:assets/dist/css --no-source-map",
    "estilos:dev": "sass src/sass:assets/dist/css --watch --no-source-map",
    "start": "sass src/sass:assets/dist/css --no-source-map && node .",
    "dev": "nodemon ."
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.3.4",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "express-handlebars": "^6.0.7",
    "sass": "^1.57.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.20"
  }
}
